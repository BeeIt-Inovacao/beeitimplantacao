# ============================================================================
# BeeIT OS-RT v2 — Template de variáveis de ambiente LOCAIS
# ----------------------------------------------------------------------------
# Copie este arquivo para .env (ignorado pelo .gitignore) e preencha.
# Usado por scripts/setup-tenant.js e utilitários futuros.
#
# ⚠️  NUNCA commit o .env real. Service-role key bypassa RLS inteiramente.
# ============================================================================

# Supabase project (via Dashboard → Project Settings → API)
SUPABASE_URL=https://dbaqvoatopfquaqgdptk.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Protheus (usado só para testes locais ad hoc — o valor de produção
# vive no Supabase Vault, nunca aqui)
PROTHEUS_TEST_USER=
PROTHEUS_TEST_PASS=
